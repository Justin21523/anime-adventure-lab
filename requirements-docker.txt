# Docker-friendly requirements (CPU-safe by default)
#
# Notes:
# - We intentionally exclude `xformers` here because it is frequently CUDA- and build-toolchain
#   sensitive and can fail to install in generic Docker environments.
# - If you have a CUDA-enabled base image/toolchain and want xformers, install it separately.
#
# Keep in sync with `requirements.txt` when adding core runtime deps.

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async & Jobs
celery[redis]>=5.3.0
redis>=5.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# AI Core
torch>=2.1.0
torchvision
transformers>=4.41.0
accelerate>=0.30.0
peft>=0.10.0
bitsandbytes>=0.41.0

# RAG & Embeddings
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# Chinese Processing
opencc-python-reimplemented>=0.1.7
pypinyin>=0.48.0

# Image Processing
pillow>=10.0.0
opencv-python>=4.8.0
rembg>=2.0.50
onnxruntime>=1.16.0

# Diffusion Models
diffusers>=0.24.0

# Utilities
datasets>=2.15.0
pandas>=2.1.0
numpy>=1.24.0
psutil>=5.9.0
pyyaml>=6.0.1
python-multipart>=0.0.6
httpx>=0.25.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.10.0
ruff>=0.1.6
pre-commit>=3.5.0
