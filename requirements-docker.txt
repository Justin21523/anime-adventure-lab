# Docker requirements — API container (CPU-only, no GPU deps)
#
# The API container runs FastAPI + orchestration logic only.
# Heavy AI workloads (T2I, VLM, training) run on the host via Celery workers.
#
# Keep torch/transformers out of the Docker image to keep it lean.
# Install them in the host conda env (rocm-pytorch-r9700).

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Async & Jobs
celery[redis]>=5.3.0
redis>=5.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# RAG & Embeddings (CPU-only)
faiss-cpu>=1.7.4
rank-bm25>=0.2.2

# Chinese Processing
opencc-python-reimplemented>=0.1.7
pypinyin>=0.48.0

# Image Processing (CPU)
pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
pandas>=2.1.0
numpy>=1.24.0
psutil>=5.9.0
pyyaml>=6.0.1
python-multipart>=0.0.6
httpx>=0.25.0

# Development (included in Docker for testing)
pytest>=7.4.0
pytest-asyncio>=0.21.0
